# KindlePool Relayer Configuration

# Secret key for the fee-bump relayer account (must have XLM balance)
KINDPOOL_RELAYER_SECRET=

# Stellar RPC URL
KINDPOOL_RPC_URL=https://soroban-testnet.stellar.org

# Network passphrase (testnet or mainnet)
KINDPOOL_NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# Port
KINDPOOL_RELAYER_PORT=3002

# Rate limit per IP per minute
KINDPOOL_RELAYER_RATE_LIMIT=50

# Max fee (stroops) for the fee-bump transaction
KINDPOOL_RELAYER_FEE=100000

# Transaction timebound TTL in seconds for the fee-bump
KINDPOOL_RELAYER_TXB_TTL_SECONDS=300

# Comma-separated allowlist of G... addresses permitted to use /relay.
# Leave empty to allow any address (mainnet: set this explicitly).
KINDPOOL_RELAYER_ALLOWLIST=
